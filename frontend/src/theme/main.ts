import { extendTheme } from "@chakra-ui/react";

export const theme = extendTheme({
  colors: {
    text: {
      "normalText": "#fff",
      "dangerText": "#c9302c",
      "dangerBackground": "#ac2925",
      "normalButtonBorder": "#fdfffc"
    },
  }
})